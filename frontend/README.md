# Frontend (File Organizer Pro)

React + Vite client for the File Organizer Pro pipeline UI.

## Setup
npm install

## Run
npm run dev

## Build
npm run build

## Tests
npm run test

## Notes
- API base URL is configured in `services/api.ts`
- Local config is persisted to `localStorage`




